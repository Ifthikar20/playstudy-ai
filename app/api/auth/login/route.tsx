// app/api/auth/login/route.tsx
import { NextResponse } from "next/server";

export async function GET() {
  // Placeholder for Google OAuth redirect
  return NextResponse.json({ message: "Google OAuth to be implemented later" });
}